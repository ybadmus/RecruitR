<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @recruiter.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @recruiter.email %>
</p>

<p>
  <table>
    <thead>
      <tr>
        <th>Assigned positions</th>
      </tr>
    </thead>

    <tbody>
      <% @recruiter.positions.each do |position| %>
        <tr>
          <td><%= position.name %></td>
        </tr>
      <% end %>
    </tbody>
</table>
</p>

<%= link_to 'Edit', edit_recruiter_path(@recruiter) %> |
<%= link_to 'Destroy', @recruiter, method: :delete, data: { confirm: 'Are you sure?' } %>
